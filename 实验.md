# HumanoidVerse

代码位置：`/inspire/qb-ilm/project/robot-reasoning/public/cyh/co_workbench/workbench/decision_ml/big_homework/`

## 实验流程

### baseline

```sh
# 环境，里面装了 humanoidverse 和 genesis
conda activate /inspire/qb-ilm/project/robot-reasoning/public/cyh/.global_envs/humanoidverse
# 或者 /inspire/qb-ilm/project/robot-reasoning/public/zhetao/HumanoidVerse/hgen

# 训练(可以跑)，默认 headless=True
bash train_genesis_baseline.sh
bash train_genesis_baseline.sh --viusal

# 测试
bash eval_baseline.sh
```

分阶段训练

```sh
# stage1：只训练往前走，建议 10000 步
bash train_genesis_pro_staged.sh --stage 1 

# stage2，这里只放开肩 pitch，不追求明显摆臂，建议 5000 步
bash train_genesis_pro_staged.sh --stage 2 checkpoint=/path/to/stage1/model_x.pt algo.config.load_optimizer=False

# stage3，加入摆臂相位 reward，建议 5000 步
bash train_genesis_pro_staged.sh --stage 3 checkpoint=/path/to/stage2/model_x.pt algo.config.load_optimizer=False

# eval
bash eval_genesis_pro_staged.sh /path/to/model_x.pt

```

## 问题

### genisis 问题

**渲染问题无解**，训练可以 headless 训练，测试只能在本地有渲染功能的卡上跑

另外还有 pyglet 的**字体编码问题**：`pyglet.window.xlib.XlibException: Could not create UTF8 text property`

可以用这个简单的脚本进行测试，如果能正常使用，应该会看到一个框一闪而过

```py
import pyglet
# print(pyglet.window.xlib._have_utf8)
print("\nYES\n")
pyglet.window.Window()
```

建议分别在系统和 conda 环境中进行测试：

```py
python - <<'PY'
import os, locale, ctypes, ctypes.util
print({k: os.environ.get(k) for k in ("LANG", "LC_ALL", "LC_CTYPE", "XLOCALEDIR", "DISPLAY")})
print("python locale:", locale.setlocale(locale.LC_CTYPE, ""))

x11 = ctypes.CDLL(ctypes.util.find_library("X11") or "libX11.so.6")
x11.XSupportsLocale.restype = ctypes.c_int
x11.XSetLocaleModifiers.argtypes = [ctypes.c_char_p]
x11.XSetLocaleModifiers.restype = ctypes.c_char_p
print("XSupportsLocale:", x11.XSupportsLocale())
print("XSetLocaleModifiers:", x11.XSetLocaleModifiers(b"@im=none"))
PY
```

如果输出 `XSupportsLocale: 0`，说明当前进程里的 Xlib 不支持当前 LC_CTYPE locale

- 系统有问题：

```sh
sudo apt-get update
sudo apt-get install -y locales libx11-6 libx11-data
sudo locale-gen en_US.UTF-8
sudo update-locale LANG=en_US.UTF-8
unset XLOCALEDIR
```

- 系统没问题但 conda 环境有问题：`export XLOCALEDIR=/usr/share/X11/locale`，将 XLOCALEDIR 指向系统 X11 locale 数据即可

### 常用

```sh
# 初始化 mamba 环境
mamba shell init --shell bash --root-prefix=/root/miniforge3

source ~/.bashrc
tmux set mouse on
source ./hgen/bin/activate

pip install -e . --index-url http://nexus.sii.shaipower.online/repository/pypi/simple --extra-index-url https://pypi.org/simple
```
